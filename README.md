# MEDI PdM — 석유화학 설비 예지보전 (dataPARC 연동)

여러 계기·전기·센서 신호를 **복합적으로 분석**해 설비 열화를 조기에 잡는 웹 기반 예지보전 도구입니다.
생산팀이 알려주기 전에 정비부서가 먼저 아는 것이 목표입니다.

- **100% 룰베이스로 동작** — API 키·인터넷·서버 없이 통계 기법만으로 진단 (AI는 추후 선택 기능)
- **의존성 0** — 빌드 없음, CDN 없음. `index.html`을 브라우저로 열면 끝 (공장 내부망 PC OK)
- **dataPARC 연동** — REST / OPC UA / PARCdata SQL / CSV 4가지 경로 지원 (게이트웨이 포함)
- **석유화학 설비 12종 커버** — 펌프·원심/왕복동 압축기·열교환기·냉각탑·제어밸브·증류탑·분해로·전동기·인버터(VFD)·변압기, 설비별 물리 지표 + ISO 14224 고장모드 라이브러리
- **논문 검증 기법 탑재** — Matrix Profile(ICDM 2016)·Isolation Forest(ICDM 2008)·ECOD(TKDE 2022)·PELT(JASA 2012)·지수 열화 RUL(Gebraeel 2005)

## 빠른 시작

### ① 데모 (설치 불필요)
`index.html`을 브라우저로 열기 (또는 GitHub Pages 배포 URL 접속).
기본으로 **P-101A 베어링 마모**, **E-301 열교환기 파울링**, **FV-101 제어밸브 스틱션**이
진행 중인 7일치 데이터가 로드되어 대시보드에서 감지 → 진단 → 알람 → 리포트 흐름을 바로 볼 수 있습니다.
설비 12기의 다른 고장(서지, 플러딩, 코킹, 변압기 누유, VFD 냉각 열화 등)도 설정에서 시나리오로 주입 가능.

### ② 내 업무 데이터로 분석 (CSV)
dataPARC(PARCview/Excel 애드인)에서 CSV 내보내기 → 웹앱 `설정 → CSV 업로드`.
`Time,TAG1,TAG2,…`(wide) 또는 `Time,Tag,Value`(long) 형식 자동 인식.
"분석 실습(5패턴)" 메뉴에서 회귀·분류·군집·이상탐지·시계열 분석을 바로 실행.

### ③ 현장 상시 연동 (게이트웨이)
dataPARC에 접근 가능한 공장 PC에서:
```bash
cd backend
pip install -r requirements.txt
cp config.example.yaml config.yaml   # 커넥터/태그 매핑 수정
uvicorn main:app --host 0.0.0.0 --port 8137
```
웹앱 `설정 → 현장 게이트웨이 → URL 입력 → 연결 테스트`. 상세: [backend/README.md](backend/README.md)

## 주요 기능

| 메뉴 | 내용 |
|---|---|
| 대시보드 | 설비 12기 건강지수(0–100) 게이지, 의심 고장모드, 활성 알람 |
| 설비 상세 | 신호 트렌드(베이스라인 밴드), **다변량 T²/SPE 관리도**, 이상 기여 태그, **최신 기법 패널**(iForest·ECOD 합의, PELT 열화 시작점, Matrix Profile 형태 이상, RUL 근사), ISO 14224 고장모드 후보+권고조치 |
| 트렌드 분석 | EWMA 관리한계·런규칙 위반 오버레이, 신호 상관 히트맵 |
| 분석 실습 | **5대 분석 패턴**(회귀/분류/군집/이상탐지/시계열)을 현재 데이터로 직접 실행 — 이상탐지는 Mahalanobis/iForest/ECOD 알고리즘 전환 비교 가능 |
| 알람/이벤트 | ISA-18.2 합리화(지속성·오프딜레이·first-out 그룹핑), 우선순위·근거·권고 포함 |
| 자산 온톨로지 | ISA-95 계층 + ISO 14224 고장모드 라이브러리 + ISA-5.1 태그 자동분류, JSON 편집 |
| 진단 리포트 | **룰베이스 한국어 종합 리포트**(기본) + AI 분석(선택, API 키 입력 시) |

## 계기(트랜스미터) 건전성 진단

공정 이상과 **계기 자체 고장**을 분리 진단합니다 — 계기 담당이 생산팀보다 먼저 아는 것이 목표.
스마트 트랜스미터 자가진단과 같은 신호 시그니처를 히스토리안 측에서 검사하고, NAMUR NE 107 분류로 표시합니다.

| 검출 | 시그니처 | 같은 원리의 벤더 진단 (현장 교차확인용) |
|---|---|---|
| 임펄스라인 막힘/동결 | 고주파 노이즈(1차 차분 σ) 붕괴 + 평균 유지 | Emerson Rosemount 3051S **SPM**(옵션 DA2) · Yokogawa EJX **ILBD**(옵션 /DG6, 고압/저압/양측 구분) · ABB 266 **PILD** |
| 출력 고착(stuck) | 장시간 연속 동일값(flatline) — 개도 신호는 스틱션과 감별 위해 장기 기준 | NE 107 F(Failure) |
| 드리프트(영점/스팬) | 한 태그만 단조 이동 + 연관 태그 정지 + 고장모드로 설명 불가 | Rosemount 3144P **Sensor Drift Alert**/Hot Backup · ABB TTH300 드리프트 감시 · Yokogawa YTA 이중센서 |
| 결선/EMI 노이즈 | 한 태그만 스파이크 반복(다른 태그 조용) | NE 107 M(Maintenance Required) |

- 검출 시: 대시보드 카드에 🔧 표시, 설비 상세에 근거·확인 순서·벤더 레퍼런스 카드, **중간 우선순위 알람** 별도 채널
- SPE(다변량) 알람의 최대 기여 태그에 계기 이상이 있으면 "계기 원인 먼저 확인" 주석 자동 부착
- 자산 온톨로지 화면에 Emerson·Yokogawa·ABB 기종별 진단기능 레퍼런스 표(공식 문서 검증 표기)
- 데모 시나리오: `PT-202 임펄스라인 막힘`, `TT-113 출력 고착` (설정에서 활성화)

## 밸브 진단 (제어밸브 + 온오프/차단밸브)

밸브는 비계획 정지의 단골 원인이라 전용 진단 층을 둡니다 — 전부 동료심사 논문/표준 실무 기법:

| 대상 | 기법 | 출처 | 산출물 |
|---|---|---|---|
| 제어밸브 진동 | ACF 영교차 규칙성 (r > 1 = 규칙 진동) | Thornhill, Huang & Zhang, *J. Process Control* 2003 | 진동 여부·주기·규칙성 r |
| 스틱션 정량화 | PV-OP 타원 적합 → OP축 폭 = **겉보기 스틱션(%)** | Choudhury, Shah & Thornhill, *Control Eng. Practice* 2006 | "스틱션 6.7%" — 3% 초과 시 정비 검토 통례 |
| 밸브 마모 지표 | 누적 이동량·방향 반전 카운트/일 | 포지셔너 진단(ValveLink 등) 표준 지표 | 헌팅/과도 사이클링 검출 |
| 차단밸브 열화 | **스트로크 시간 추세** + 구동 공기압 | PST 실무 (IEC 61511; Lundteigen & Rausand 2008) | "8초→17초, +22σ" → 고착 전 계획 정비 |
| 차단밸브 작동성 | **지령-리미트(ZSO/ZSC) 정합** 감시 | SIS 실무 | 지령 불응 즉시 검출 — ESD 안전기능 상실 방지 |

- 데모: FV-101 스틱션(정량화 포함), XV-701 스트로크 지연·작동 불능 시나리오
- 관련 벤더 진단(교차 확인용, 검증됨): Fisher FIELDVUE DVC6200/ValveLink·DVC6200 SIS PST, Valmet Neles ND9000(부하율), SAMSON EXPERTplus/TROVIS SAFE, Neles ValvGuard VG9000

## 전기/디지털 신호 (트립 · 릴레이 · 알람유닛)

보호계전기 트립, Aux Relay, 알람유닛 접점 같은 **0/1 신호**는 아날로그와 문법이 달라 전용 진단을 씁니다.
XA(알람접점)·XS(상태접점) 태그로 히스토리안에 수집하면 어느 제조사 계전기든 동일하게 동작합니다.

| 검출 | 판정 | 알람 |
|---|---|---|
| 보호 트립 (86 록아웃) | 접점 1 = 래치 상태(리셋 전) | **긴급** — "원인 규명·리셋 전 재기동 금지" + 정지 설비의 통계 알람 전체 자동 억제(상태기반 억제) |
| 알람 접점 (49 열동 등) | 접점 1 + 활성시간 표시 | 높음 — 트립 선행 경고 |
| 접점 채터링 | 시간당 상태변화가 평시 대비 급증 | 중간 — 결선 이완·접점 마모·코일전압 marginal의 대표 증상 |

- ANSI/IEEE C37.2 디바이스 번호 기준(49/50/51/27/59/46/66/86/87/94) — 도면·계전기 표기와 그대로 호환
- 데모 시나리오: `M-401 과열→49 알람→86 트립`(선행 징후→트립 래치→정지·냉각 전 과정), `Aux Relay 채터링`

## 현장 시스템 연동 레퍼런스 (검증됨)

| 시스템 | 연동 포인트 |
|---|---|
| **Bently Nevada 3500** (API 670) | overall 진동을 히스토리안으로 → 본 시스템이 추세 이상 검출 → System 1에서 스펙트럼 정밀진단. 진동값이 갑자기 0 근처면 프로브/케이블(OK 리밋·갭 전압 -9~-11V) 먼저 의심 |
| **Atlas Copco** (Elektronikon Mk5/Nano) | 토출온도·부하율을 Modbus로 수집 → 압축기 로직 적용. SMARTLINK 원격감시 병행 |
| **Yokogawa CENTUM VP / ProSafe-RS** | 알람·이벤트는 Exaopc(OPC A&E)·Exaquantum 경유. 트립 정밀 사건순서는 SIS의 SOE 기록으로 교차 확인 |
| **ABB 보호계전기** (Relion REF615/REM615·구형 REF/REM54x) | 트립·알람 접점(XA)과 **계전기 아날로그 값**(49 열용량 사용률 % 등)을 함께 수집 → 트립이 뜨기 **전에** 열용량 상승으로 선행 경고. IEC 61850/Modbus → 게이트웨이 경유 |
| **모터 보호계전기** (GE Multilin 869 등) | 트립/알람 a·b접점을 XA 태그로 수집 → 아날로그 선행 징후(권선온도·전류)와 연계 분석 |

## 데이터 준비 — 태그 검색 · 조합 · 필요 신호 추천

목표는 **비계획 정지(인터록 트립) 예방**입니다. 그 첫 단계는 올바른 신호 조합:

1. **태그 브라우저**: 히스토리안에서 온 태그를 검색(부분일치)·선택. ISA-5.1 자동분류·소속 표시
2. **필요 신호 추천**: 설비 종류를 고르면 "진단에 필요한 신호"를 필수/권장으로 추천 —
   예: 전동기 → 전류·권선온도·열용량(필수), 진동·베어링온도. 각 신호가 **어떤 고장모드 검출에 쓰이는지** 근거 표시
   (고장모드 라이브러리에서 자동 도출 — 계기 신설/수집 요청의 근거 자료로 활용)
3. **조합 만들기**: 선택 태그를 역할에 매핑(ISA 분류로 자동 제안) → "내 조합"이 대시보드·백테스트에서 기존 설비와 동일하게 진단됨.
   필수 신호가 빠지면 어떤 감별력이 낮아지는지 경고

## 백테스트 — "미리 잡을 수 있었나" 검증

실제 발생했던 고장/트립 데이터(CSV 업로드 or 게이트웨이 과거 조회)로 시스템을 사후 검증합니다:

- 시간 순 재생: 각 평가 시점에서 **그때까지의 데이터만** 사용 (미래 누수 없음)
- 캘리브레이션 구간(기록 앞 40%, 최대 7일)을 정상 기준으로 고정 학습 → 이후 전진 평가
- 실제 고장 시각 입력 시 **리드타임** 산출: "실제 트립 130시간 전 첫 알람 — 계획 정비 가능했음"
- 경고가 안 떴으면 → 필요 신호 추천으로 계측 보강 안내 (데모 검증: 베어링 고장 리드타임 130h, 정상 데이터 오탐 0)

## 연결 신뢰성 — "제대로 불러온다"를 보장하는 3중 장치

dataPARC 연동은 공식 OpenAPI 스펙(github.com/dataPARC/store)으로 재검증했고, 그래도 현장은 다르므로 자가진단을 내장했습니다:

1. **연결 진단 마법사** (설정): 브라우저 사전점검(혼합콘텐츠) → 게이트웨이 도달 → 커넥터 → 태그 목록 →
   샘플 읽기 → 타임스탬프 신선도/순서 → PC-게이트웨이 시각 동기까지 단계별 ✅/❌ + **실패 단계별 현장 확인 순서** 제시
2. **데이터 품질 검증** (데이터 준비, 매 로드 자동): 수집 공백·정체(stale)·NaN·타임스탬프 역순·
   전구간 고정 · **스케일/단위 의심**(0-1 vs 0-100%, ×10 환산) 자동 검출
3. **계산 태그** (값 응용): 안전한 수식 엔진(eval 불사용) — `[FT-101]*0.16667`(단위환산),
   `[PT-102]-[PT-101]`(차압), `max([TT-103],[TT-104])`. 스케일 오류 보정·파생값 생성을 코드 수정 없이.
   가상 태그는 트렌드·조합·백테스트에서 실제 태그처럼 동작

핵심 검증 사실 (공식 스펙 확인 — 연동 실패의 단골 원인):
- dataPARC.Store REST의 read는 **숫자 태그 ID(int32)만** 받음 — 이름을 넘기면 실패.
  게이트웨이 커넥터가 `"Group/Interface/TagName"` → ID 자동 해석 (config.yaml 두 형태 지원)
- 타임스탬프는 UTC "Z" — 로컬 시각 혼동 시 "최근 데이터 없음"으로 보임 (마법사가 시각 동기 검사)
- 보안 확인은 `GET /auth-info` (루트 경로), HTTPS 전용, 구형 PARChistory는 OPC UA(51235) 폴백
- 설정 화면에 **증상별 트러블슈팅 표** 8종 내장 (연결 실패/태그 0개/데이터 0점/값 이상/끊김/혼합콘텐츠 등)

## 알림 · 사내망 배포

- **브라우저 알림**: 설정에서 옵트인 — 긴급/높음 알람 발생 시 데스크톱 알림 (탭이 열려 있는 동안 동작. 상시 감시가 필요하면 게이트웨이 서버 측 알림을 Phase 3에서)
- **사내망 상시 접속**: 정적 파일이므로 사내 웹서버(IIS/nginx)나 공유폴더에 두면 어디서든 열립니다.
  `config.example.js`를 `config.js`로 복사해 게이트웨이 주소를 **한 번만 하드코딩**하면 모든 PC에 공통 적용
  (개인이 설정 화면에서 저장한 값이 우선). 과거 데이터 기간(일)도 설정에서 조정 — 백테스트는 길수록 좋음

## 진단 원리 쉬운 설명 (UI 내장)

모든 진단 패널에 **❓ 쉽게 설명** 접이식 박스가 있습니다 — 비유 중심 설명(예: CUSUM = "조금씩 넘친 양을 저금통에 모아 임계를 넘으면 경보", Isolation Forest = "스무고개에서 몇 번 만에 혼자 고립되면 이상치", 86 트립 = "스스로 안 풀리는 자물쇠"). 건강지수 계산법, 알람이 적게 뜨는 설계 의도(first-out·상태기반 억제)까지 화면 안에서 바로 확인됩니다.

## 커버하는 설비 (데모 12기)

| 설비 | 클래스 | 물리 지표 | 대표 고장모드 |
|---|---|---|---|
| P-101A/B 원심펌프 | CP | 효율 프록시 Q·ΔP/I | 베어링 마모, 캐비테이션, 임펠러 마모 |
| FV-101 제어밸브 | CV | OP-개도 편차 진동, 루프 진동성 | 스틱션, 액추에이터 고장, 포지셔너 |
| C-201 원심압축기 | CO | 서지마진, 압축비 보정 토출온도 | 서지 접근, 내부 오염 |
| C-202 왕복동압축기 | RC | 단열 토출온도 잔차 T_d−T_s·r^((k−1)/k) | 밸브 누설(비계획정지 1위), 패킹 누설 |
| E-301 열교환기 | HE | U값 프록시 Q/LMTD, 접근온도차 | 파울링, 튜브 누설 |
| CT-601 냉각탑 | CT | 접근온도차, 효율 R/(R+A) | 충전재 오염, 팬/기계 계통 |
| TR-101 변압기 | TR | 부하(K²) 보정 유온상승 잔차 (IEEE C57.91) | 누유, 냉각 열화, 절연 열화, H₂ 가스 |
| VFD-401 인버터 | VF | I² 보정 방열판 온도 잔차, f²-전류 잔차 | 냉각(팬/필터) 열화, DC버스 캐패시터, 기계측 과부하 |
| M-401 전동기 | EM | I² 보정 권선온도 잔차 | 권선 절연 열화, 베어링(전식 포함) |
| F-501 분해로 | FH | TMT−COT 갭, 배기 O₂ | 튜브 코킹, 대류부 오염, 버너 이상 |
| T-401 증류탑 | DC | 유량 정규화 ΔP, 온도 프로파일 붕괴 | 플러딩 접근, 트레이 오염, 위핑 |

## 어떻게 "복합적으로" 잡나

1. **단변량 SPC**: EWMA·CUSUM·런규칙으로 개별 신호의 서서한 이동/추세 검출
2. **다변량 PCA**: 개별 태그가 전부 정상범위여도 **신호 간 상관구조가 무너지면**(SPE↑) 경보 — 단순 임계값으로 못 잡는 유형
3. **물리 파생지표**: 부하로 설명되는 변화를 제거한 잔차(방열판 온도 vs I², 유온상승 vs K² 등) — 부하 변동 오경보를 구조적으로 차단
4. **고장모드 매칭**: 관측 증상 조합을 ISO 14224 라이브러리와 대조 → "베어링 마모 85% 일치, 권고: 오일 분석·진동 정밀진단". 미관측 감별 증상은 점수 차감(증거 가중 수축)
5. **검증 기법 합의**: Isolation Forest와 ECOD가 **둘 다** 최근 구간을 이상으로 판정하고 다른 근거(SPE/고장모드)와 일치할 때만 건강지수에 반영 — 단일 알고리즘 오탐 억제
6. **열화 시점·잔여수명**: PELT 변화점으로 열화 시작 시점을 찾고, 그 이후 구간에 지수 열화 모델을 적합해 상한 도달 시점(RUL)을 근사

## 저장소 구조

```
index.html            웹앱 진입점 (그대로 열면 동작)
css/, js/             앱 (분석엔진 js/analytics/, 온톨로지, 시뮬레이터, 차트, 리포트)
backend/              현장 게이트웨이 (FastAPI + dataPARC 커넥터 4종)
docs/PLAN.md          설계 문서 (아키텍처·분석기법·dataPARC 연동 조사결과·로드맵)
tests/                분석엔진 단위테스트 (node tests/analytics.test.js)
```

## 적용 논문 (전부 동료심사 검증 기법)

| 기법 | 출처 | 이 시스템에서의 역할 |
|---|---|---|
| Matrix Profile (STOMP) | Yeh et al., ICDM 2016 / Zhu et al., ICDM 2016 | 파라미터 없는 시계열 형태 이상(디스코드) 탐지 |
| Isolation Forest | Liu et al., ICDM 2008 | 비선형·다봉 데이터 이상점수 (정상 베이스라인으로만 학습) |
| ECOD | Li et al., IEEE TKDE 2022 | 파라미터 0개 분포 꼬리 기반 이상탐지 — iForest와 상호 검증 |
| PELT 변화점 | Killick et al., JASA 2012 | 열화 시작 시점 검출 (평균+분산 비용, BIC 페널티) |
| 지수 열화 RUL | Gebraeel et al., IIE Trans. 2005 | 열화 시작 후 상한 도달 시점(잔여수명) 근사 |
| EWMA/CUSUM/T²/SPE | Montgomery SPC 표준, Jackson–Mudholkar 1979 | 단변량/다변량 관리도 |

## AI 분석 (선택 — 키 입력 시)

지금은 키 없이 룰베이스로 완결됩니다. `설정 → AI 분석`에서 API 키를 입력하면
자산 온톨로지+분석결과를 컨텍스트로 자연어 질의(감별진단, 정비계획)가 가능해집니다.

- **제공자 4종**: Anthropic(Claude) · OpenAI(GPT) · Google(Gemini) · **OpenAI 호환**(사내 Ollama/vLLM/LiteLLM/Azure 등 Base URL 지정)
- 모델명 자유 입력(권장 모델 자동완성), 스트리밍 응답
- 키는 기본 **브라우저 메모리에만** 보관(새로고침 시 삭제), localStorage 저장은 명시적 체크 시에만
