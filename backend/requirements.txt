# MEDI PdM 게이트웨이 — 필수 (전부 표준적·널리 검증된 저리스크 패키지만 사용)
# 오토파일럿(자동 연결/트러블슈팅)은 신규 의존성 0 — 표준 라이브러리 + httpx 재사용
fastapi>=0.110
uvicorn[standard]>=0.29
pyyaml>=6.0
httpx>=0.27

# 선택: 사용하는 커넥터만 설치
# OPC UA 커넥터 (opc.tcp://SERVER:51235/Capstone/OPCUAServer)
# asyncua>=1.1   ← 순수 파이썬, LGPL, 활발히 유지보수 — 저리스크
# PARCdata SQL 커넥터 (SQL Server + ctc_fn_PARCdata_* 함수)
# pyodbc>=5.1
